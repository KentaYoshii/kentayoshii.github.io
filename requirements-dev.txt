# Development-only dependencies. Neither is needed to build the site: the
# generators reached by build.py use nothing but the standard library, which
# is what lets CI regenerate docs/_data with no install step at all.
#
#   pytest  -- to run `python3 -m pytest`.
#   Pillow  -- only for `python3 scripts/build_gallery_thumbs.py`, the manual
#              pass that resizes gallery photos. Its output is committed, so
#              neither build.py nor CI ever needs it.
pytest>=8
Pillow>=10

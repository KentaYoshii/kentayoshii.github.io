---
layout: page
title: Books
permalink: /books/
---

{%- comment -%}
Rendered from docs/_data/books.json, which is generated by
scripts/merge_books.py (Goodreads export + the markdown logs in _posts).
The data file is pre-sorted by normalised author then title, and Liquid's
group_by preserves that order, so no sorting is needed here.
{%- endcomment -%}

<div class="wide-section"><div class="wide-inner">
<div class="collection-page" data-collection="books" data-group-label="authors">
  <div class="collection-header">
    <p class="collection-tagline">Everything I've read, grouped by author.</p>
    <input type="text" class="search-input" placeholder="Search titles or authors…" aria-label="Search books">
    <p class="stats-summary" aria-live="polite"></p>
  </div>

  {%- assign grouped = site.data.books | group_by: "author" -%}
  {%- for group in grouped -%}
  <section class="year-block">
    <h1 class="year-heading">{{ group.name }} <span class="year-count">{{ group.size }}</span></h1>
    <ul>
      {%- for book in group.items -%}
      {%- comment -%}An empty string is truthy in Liquid, so test it explicitly.{%- endcomment -%}
      <li data-author="{{ book.author | escape }}"{% if book.isbn and book.isbn != "" %} data-isbn="{{ book.isbn }}"{% endif %}><em>{{ book.title | escape }}</em>{% if book.year and book.year != "" %}<span class="entry-date"> · {{ book.year }}</span>{% endif %}</li>
      {%- endfor -%}
    </ul>
  </section>
  {%- endfor -%}
</div>
</div></div>

<script src="{{ '/assets/js/main.js' | relative_url }}" defer></script>

---
layout: page
title: Movies
permalink: /movies/
---

{%- comment -%}
Rendered from docs/_data/movies.json, generated by scripts/build_movies.py
from the markdown logs in _posts (which remain the source of truth — re-run
that script after editing them). Pre-sorted by title, leading articles
ignored; the "group by" control re-groups it client-side.
{%- endcomment -%}

<div class="wide-section"><div class="wide-inner">
<div class="collection-page" data-collection="movies">
  <div class="collection-header">
    <p class="collection-tagline">Movies and shows I've watched.</p>
    <div class="collection-controls">
      <input type="text" class="search-input" placeholder="Search titles…" aria-label="Search movies">
      <label class="group-control">Group by
        <select class="group-select" aria-label="Group movies by">
          <option value="none">Nothing</option>
          <option value="year">Year</option>
          <option value="letter">First letter</option>
        </select>
      </label>
    </div>
    <p class="stats-summary" aria-live="polite"></p>
  </div>

  <div class="collection-items" data-items>
    <section class="year-block">
      <ul>
        {%- for movie in site.data.movies -%}
        <li data-title="{{ movie.title | escape }}" data-letter="{{ movie.letter }}" data-year="{{ movie.year }}">{{ movie.title | escape }}<span class="entry-date"> · {% if movie.month %}{{ movie.month | slice: 0, 3 }} {% endif %}{{ movie.year }}</span></li>
        {%- endfor -%}
      </ul>
    </section>
  </div>
</div>
</div></div>

<script src="{{ '/assets/js/main.js' | relative_url }}" defer></script>

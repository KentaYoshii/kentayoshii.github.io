---
layout: page
title: Travel
permalink: /travel/
description: A passport-style checklist of every US National Park, and which ones I've actually been to.
---

{%- comment -%}
Rendered from docs/_data/travel.json, generated by scripts/build_travel.py
from the fixed list in scripts/national_parks.py plus the visits logged in
_logs/travel.md. Unlike Books/Movies this needs no client-side search or
grouping — 63 tiles fit on one screen — so the whole page is server-rendered
and works with no JavaScript at all.
{%- endcomment -%}

<div class="wide-section"><div class="wide-inner">
<div class="travel-page">
  <p class="collection-tagline">
    {{ site.data.travel.national_parks.visited }} of {{ site.data.travel.national_parks.total }}
    US National Parks visited.
  </p>

  <div class="park-grid">
    {%- for park in site.data.travel.national_parks.parks -%}
    <div class="park-tile{% if park.visited %} is-visited{% endif %}">
      <span class="park-name">{{ park.name }}</span>
      <span class="park-state">{{ park.state }}</span>
      {%- if park.visited -%}
      <span class="park-stamp">
        {%- if park.month %}{{ park.month | slice: 0, 3 }} {% endif -%}{{ park.year }}
      </span>
      {%- endif -%}
    </div>
    {%- endfor -%}
  </div>
</div>
</div></div>

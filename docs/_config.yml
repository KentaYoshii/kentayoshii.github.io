# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Kenta's Log
description: A running log of books I've read, movies/shows I've watched, and dev notes I don't want to forget.
author: Kenta Yoshii

# Link previews. The card is generated from the shelf by
# scripts/build_images.py, so it changes as the shelf grows.
# jekyll-seo-tag reads page.image and does not fall back to site.image, so
# this has to be a front-matter default rather than a plain site setting.
defaults:
  - scope:
      path: ""
    values:
      image: /assets/social-card.png
twitter:
  card: summary_large_image
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://kentayoshii.github.io" # the base hostname & protocol for your site, e.g. http://example.com

# Build settings
theme: minima
plugins:
  - jekyll-feed
  # Generates the <title>, description and Open Graph/Twitter tags that
  # link previews read. minima depends on it, so it loaded implicitly
  # before; listing it here makes that explicit.
  - jekyll-seo-tag

# Controls the order of pages in the site nav (minima reads this list)
header_pages:
  - books.markdown
  - movies.markdown
  - travel.markdown
  - posts.markdown
  - dev.markdown
  - stats.markdown

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
